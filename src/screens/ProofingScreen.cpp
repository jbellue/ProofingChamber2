// This file intentionally left minimal. `ProofingScreen` is now an alias
// for `ProofingController` (see `src/screens/ProofingScreen.h`). The
// controller and view implementations live in `screens/controllers` and
// `screens/views` respectively.

#include "ProofingScreen.h"
